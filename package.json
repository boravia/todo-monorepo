{
  "name": "todo-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"yarn workspace client dev\" \"yarn workspace server start:dev\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
